"use client";

/**
 * ParticleMorphPlayground — alias for SVGParticleMorphViz.
 *
 * The full interactive playground (shape presets, custom SVG input,
 * physics sliders, code export) lives in SVGParticleMorphViz.tsx.
 * This re-export keeps both component names available in MDX.
 */
export { default } from "./SVGParticleMorphViz";
